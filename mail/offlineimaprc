[general]
# keep quiet for cronjob
ui = quiet
# ui for debugging and feedback
# ui = TTYUI
accounts = CaffeinatedBrad, jones.bradley, bj59, bradjone
pythonfile = ~/.mutt/getpasswords.py
fsync = False

# You can have offlineimap continue running indefinately, automatically
# syncing your mail periodically. If you want that, specify how
# frequently to do that (in minutes) here. You can also specify
# fractional minutes (ie, 3.25).
autorefresh = 3.25
quick = 10



[Account CaffeinatedBrad]
localrepository = CaffeinatedBrad-Local
remoterepository = CaffeinatedBrad-Remote
status_backend = sqlite

[Repository CaffeinatedBrad-Local]
type = Maildir
localfolders = ~/.mail/caffeinatedbrad
nametrans = lambda folder: {'drafts':  '[Gmail]/Drafts',
                            'sent':    '[Gmail]/Sent Mail',
                            'flagged': '[Gmail]/Starred',
                            'trash':   '[Gmail]/Trash',
                            'archive': '[Gmail]/All Mail',
                            }.get(folder, folder)

[Repository CaffeinatedBrad-Remote]
maxconnections = 1
type = Gmail
remoteuser = caffeinatedbrad@gmail.com
remotepasseval = get_password("caffeinatedbrad@gmail.com")
realdelete = no
nametrans = lambda folder: {'[Gmail]/Drafts':    'drafts',
                            '[Gmail]/Sent Mail': 'sent',
                            '[Gmail]/Starred':   'flagged',
                            '[Gmail]/Trash':     'trash',
                            '[Gmail]/All Mail':  'archive',
                            }.get(folder, folder)
folderfilter = lambda folder: folder not in ['[Gmail]/Trash',
                                             '[Gmail]/Important',
                                             '[Gmail]/Spam',
                                             ]
keepalive = 60
holdconnectionopen = yes


[Account jones.bradley]
localrepository = jones.bradley-Local
remoterepository = jones.bradley-Remote
status_backend = sqlite

[Repository jones.bradley-Local]
type = Maildir
localfolders = ~/.mail/jones.bradley

[Repository jones.bradley-Remote]
type = IMAP
ssl = yes
remotehost = imap.mail.me.com
remoteuser = jones.bradley@me.com
remotepasseval = get_password("jones.bradley@me.com")
keepalive = 60
holdconnectionopen = yes

[Account bj59]
localrepository = bj59-Local
remoterepository = bj59-Remote
status_backend = sqlite

[Repository bj59-Local]
type = Maildir
localfolders = ~/.mail/bj59

[Repository bj59-Remote]
maxconnections = 1
type = IMAP
ssl = yes
remotehost = pod51036.outlook.com
remoteuser = bj59@kent.ac.uk
remotepasseval = get_password("bj59@kent.ac.uk")
folderfilter = lambda folder: folder not in ['Calendar',
                                             'Contacts',
                                             'Notes',
                                             'Journal',
                                             'Tasks',
                                             'Deleted Items',
                                             'Deleted Messages',
                                             'Outbox',
                                             'Sent Items',
                                             ]
keepalive = 60
holdconnectionopen = yes

[Account bradjone]
localrepository = bradjone-Local
remoterepository = bradjone-Remote
status_backend = sqlite

[Repository bradjone-Local]
type = Maildir
localfolders = ~/.mail/bradjone

[Repository bradjone-Remote]
type = IMAP
ssl = yes
remotehost = mail.cisco.com
remoteuser = bradjone@cisco.com
remotepasseval = get_password("bradjone@cisco.com")
folderfilter = lambda folder: folder not in ['Calendar',
                                             'Contacts',
                                             ]
